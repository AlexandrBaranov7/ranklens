# Лог работы с LLM-ассистентом

> Временный артефакт разработки, удаляется перед релизом (CONTRIBUTING, раздел 11).

Требование курса: что предложил ассистент, что поправлено вручную и почему.
Запись — на каждый значимый PR, сразу после ревью. Правила — в CONTRIBUTING, раздел 8.

Формат записи:

```
## <дата> — <PR / тема>
Ассистент: какой, в каком режиме.
Предложено: ...
Не устроило / переписано вручную: ... — почему.
Что проверено самостоятельно: ...
```

---

## 2026-09-16 — ревью плана, каркас репозитория, правила разработки

**Ассистент:** Claude Code (Claude Opus 5), агентный режим в локальном репозитории.

**Предложено ассистентом:**

- Ревью плана проекта (ведётся вне репозитория): найдены ошибки в инвариантах тестов (NDCG@k не монотонна по k),
  расхождения с `pytrec_eval` (линейный gain, пересортировка по score, знаменатель AP),
  проблема с `frozen` dataclass и полем-списком, нарушение правила «`core` только stdlib»
  протоколом с `ndarray`, утечка дескриптора в `AtomicReport` из `mkstemp`.
  Открытые вопросы вынесены в `docs/decisions.md`.
- src-layout, ruff/mypy/pytest-конфигурация, pre-commit, CI. Сначала — hatchling и pip;
  по предложению автора переведено на Poetry (D-002).
- CONTRIBUTING с правилами процесса, границами слоёв и правилами кода.
- `core/types.py` (tuple вместо list в `RankedList` — отступление от первоначального дизайна, D-007),
  `core/exceptions.py` (pickle-совместимые исключения, D-008), архитектурный тест (D-009).

**Не устроило / переписано вручную:** внутри проекта отсутствует план, но упоминается, внутри плана, находящегося локально, отсутствует техдолг на выпиливание логов перед релизом.
PR №2 типы данных, иммутабельность, датакласс, где, возможно, стоит использовать ванильный класс (без соответствующего декоратора)

**Проверено самостоятельно:** проверены соглашения о правилах проекта, проблем нет, апрув.

## 2026-09-16 — неделя 4: ErrorCollector, форматы, iter_run

**Ассистент:** Claude Code (Claude Opus 5), агентный режим.

**Предложено ассистентом:** `ErrorCollector` с примерами, ограниченными
**на каждый тип** ошибки (а не первые N вообще — иначе редкий тип ошибки
не попадёт в сводку за частым); отдельные `UngroupedInputError` и
`MissingColumnError` вместо переиспользования `UnsortedInputError`/`MalformedRowError`
с неточным текстом; иерархия предупреждений для non-strict режима.
Форматы: TREC сверх плана, разделение на слой форматов и слой схемы (D-012).
`iter_run`: два режима проверки порядка (D-013, закрывает Q-1); замер
памяти показал ~190 байт на запрос в режиме `grouped` — записано как цена
решения, а тест на память проверен мутацией (reader, копящий выдачи, его роняет).
По ревью PR #3: интерфейс коллектора сужен до `record`/`snapshot` (D-015).

**Не устроило / переписано вручную:** _заполняет автор после ревью PR_.

**Проверено самостоятельно:** _заполняет автор_.
